# 🎯 Система Toast Уведомлений - Руководство по внедрению

## ✅ Что уже готово

### 1. `ToastContext` 
- Время показа изменено на 15 секунд
- Поддерживает `success` и `error` типы
- Автоскрытие и ручное закрытие

### 2. `useApiToast` хук
- `createOperation` - для создания
- `updateOperation` - для редактирования  
- `deleteOperation` - для удаления
- `loadOperation` - для загрузки (только ошибки)

### 3. Пример на странице предметов
- Все CRUD операции покрыты уведомлениями
- Правильная обработка ошибок API

## 🚀 Как внедрить на новой странице

### Шаг 1: Импорт
```tsx
import { useApiToast } from '../../hooks/useApiToast';
```

### Шаг 2: Инициализация хука
```tsx
const { createOperation, updateOperation, deleteOperation, loadOperation } = useApiToast();
```

### Шаг 3: Замена API вызовов

#### Создание
```tsx
// Было:
await AuthenticatedApiService.post('/Entity/create', data);

// Стало:
await createOperation(
  () => AuthenticatedApiService.post('/Entity/create', data),
  'Сущность'
);
```

#### Редактирование
```tsx
// Было:
await AuthenticatedApiService.put(`/Entity/${id}`, data);

// Стало:
await updateOperation(
  () => AuthenticatedApiService.put(`/Entity/${id}`, data),
  'Сущность'
);
```

#### Удаление
```tsx
// Было:
await AuthenticatedApiService.delete(`/Entity/${id}`);

// Стало:
await deleteOperation(
  () => AuthenticatedApiService.delete(`/Entity/${id}`),
  'Сущность'
);
```

#### Загрузка
```tsx
// Было:
const data = await AuthenticatedApiService.post('/Entity/GetAll', params);

// Стало:
const data = await loadOperation(
  () => AuthenticatedApiService.post('/Entity/GetAll', params),
  'сущности'
);
```

## 📋 Список страниц для обновления

- [ ] 🏢 Организации (`/organizations`)
- [ ] 🚪 Кабинеты (`/rooms`) 
- [ ] 👥 Группы (`/groups`)
- [ ] 🗓️ Расписания (`/schedules`)
- [ ] 👨‍🎓 Студенты (`/students`)
- [ ] 📚 Уроки (`/lessons`)
- [ ] ✅ Посещаемость (`/attendance`)

## 🎨 Тексты уведомлений

### Стандартные названия сущностей:
- `Организация` / `организацию`
- `Кабинет` / `кабинет`
- `Группа` / `группу`
- `Расписание` / `расписание`
- `Студент` / `студента`
- `Урок` / `урок`
- `Посещаемость` / `посещаемость`

### Примеры результата:
- ✅ "Группа успешно создана"
- ✅ "Кабинет успешно обновлен"
- ✅ "Студент успешно удален"
- ❌ "Не удалось создать группу"
- ❌ "Не удалось загрузить кабинеты"

## 🔧 Продвинутое использование

### Кастомные сообщения
```tsx
await handleApiOperation(
  () => AuthenticatedApiService.post('/special-action', data),
  {
    successMessage: "Специальное действие выполнено!",
    errorMessage: "Не удалось выполнить действие"
  }
);
```

### Без уведомлений
```tsx
await handleApiOperation(
  () => AuthenticatedApiService.get('/silent-action'),
  {
    showSuccess: false,
    showError: false
  }
);
```